@using DotNet8Mvc.NLayerArchitecture.Models.Features
@using DotNet8Mvc.NLayerArchitecture.Models.Features.Blog

@model Result<BlogListResponseModel>;

@{
    ViewData["Title"] = "Blog Management";
    var success = TempData["success"];
    var error = TempData["error"];
}

@if (error is not null)
{
    <div class="alert alert-danger" role="alert">
        @error
    </div>
}

@if (success is not null)
{
    <div class="alert alert-success" role="alert">
        @success
    </div>
}

@if (Model.Data.DataLst.Count == 0)
{
    <h3>No Data.</h3>
}
else
{
    <a href="/Blog/CreateBlog" class="btn btn-outline-dark mb-3">Create Blog</a>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Blog Id</th>
                <th>Blog Title</th>
                <th>Blog Author</th>
                <th>Blog Content</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Data.DataLst)
            {
                <tr>
                    <td>@item.BlogId</td>
                    <td>@item.BlogTitle</td>
                    <td>@item.BlogAuthor</td>
                    <td>@item.BlogContent</td>
                    <td>
                        <a class="btn btn-outline-success" href="/Blog/EditBlog/@item.BlogId">Edit</a>
                        <button type="button" class="btn btn-outline-danger btn-delete" data-id="@item.BlogId">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@section scripts {
    <script>
        $('.btn-delete').click(function (e) {
            let result = confirm("Are you sure want to delete?");
            if (!result) return;

            let id = $(this).data('id');

            $.ajax({
                type: "POST",
                url: `/Blog/Delete/${id}`,
                data: {  },
                dataType: "json",
                success: function (data) {
                    console.log({ data });
                    alert(data.message);
                    location.href = "/Blog/Index";
                },
                error: function (ex) {
                    var r = jQuery.parseJSON(response.responseText);
                    console.log("Message: " + r.Message);
                    console.log("StackTrace: " + r.StackTrace);
                    console.log("ExceptionType: " + r.ExceptionType);
                }
            });
        })
    </script>
}